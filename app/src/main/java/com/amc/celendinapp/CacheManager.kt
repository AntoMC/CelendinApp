package com.amc.celendinapp

import android.content.Context
import com.amc.celendinapp.model.Cliente

object CacheManager {
    fun guardar(context: Context, clientes: List<Cliente>) {
        // Desactivado temporalmente para permitir compilación
    }

    fun leer(context: Context): List<Cliente>? {
        // Desactivado temporalmente para permitir compilación
        return null
    }
}
